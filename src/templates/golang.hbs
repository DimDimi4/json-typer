package main

/// Auto-generated by json-typer
/// Generated on {{ date }}

{{#each externals}}
import "{{.}}"
{{/each}}

{{#each structs}}
{{#if description}}// {{description}}{{/if}}
type {{typeName name}} struct {
    {{#each properties}}
    {{propName name}} {{{propType .}}} `json:"{{name}}{{#if isRequired}}{{else}},omitempty{{/if}}"` {{#if description}}// {{{description}}}{{/if}}
    {{/each}}
}

{{#each enums}}
{{#if description}}/// {{description}}
{{/if}}
type {{enumName name}} string

{{#each values}}
const {{enumValue .}} {{enumName ../name}} = "{{.}}"
{{/each}}

{{/each}}

{{/each}}
